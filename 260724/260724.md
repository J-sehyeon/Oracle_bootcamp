# 260724 학습노트

## 버튼을 눌렀을 때 나오는 내용을 request하는 방법
1. 개발자 도구를 킨다.
2. 버튼을 눌러 Network페이지를 본다.
3. Headers에서 url을 찾고 POST방식임을 확인한다.
4. Payload에서 Form Data에서 View Source버튼을 누른다.
5. 내용을 &를 기준으로 딕셔너리 형태로 만든다.
6. 딕셔너리를 payload에 저장 후 requests.post(url, data=payload)를 실행한다.